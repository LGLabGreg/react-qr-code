'use client'

import { ReactQRCode } from '@lglab/react-qr-code'

export const codeBlock = `
import { ReactQRCode } from '@lglab/react-qr-code'

export const Demo = () => {
  return <ReactQRCode marginSize={2} size={256} value='https://reactqrcode.com' />
}
`

export const Demo = () => {
  return <ReactQRCode marginSize={2} size={256} value='https://reactqrcode.com' />
}
